import { PinNumberHiddenPipe } from './pin-number-hidden.pipe';

describe('PinNumberHiddenPipe', () => {
  it('create an instance', () => {
    const pipe = new PinNumberHiddenPipe();
    expect(pipe).toBeTruthy();
  });
});
